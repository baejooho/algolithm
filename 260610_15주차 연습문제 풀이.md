# Chapter 08 — 그래프 (Graph)

---

## 학습 목표
- 그래프 자료구조의 개념을 이해한다
- 그래프를 표현하는 방법을 알아본다
- 그래프를 순회하는 방법과 알고리즘을 알아본다
- 신장 트리와 최소 비용 신장 트리를 알아본다
- 최소 비용 신장 트리 알고리즘을 이해한다

---

## 1. 그래프의 구조

### 그래프란?
> 연결되어 있는 원소 사이의 **다:다 관계**를 표현하는 자료구조

```
G = (V, E)
V = 정점(vertex)들의 집합
E = 간선(edge)들의 집합
```

---

### 그래프의 종류

#### ① 무방향 그래프 (Undirected Graph)
- 간선에 **방향이 없는** 그래프
- (Vi, Vj) = (Vj, Vi) → **같은 간선**

#### ② 방향 그래프 (Directed Graph, Digraph)
- 간선에 **방향이 있는** 그래프
- Vi → Vj 를 `<Vi, Vj>` 로 표현
- Vi = 꼬리(tail), Vj = 머리(head)
- `<Vi, Vj>` ≠ `<Vj, Vi>` → **다른 간선**

#### ③ 완전 그래프 (Complete Graph)
- 각 정점에서 **다른 모든 정점을 연결**한 그래프

| 종류 | 최대 간선 수 공식 |
|------|----------------|
| 무방향 그래프 | **n(n-1)/2** |
| 방향 그래프 | **n(n-1)** |

예시 (정점 4개):
- G5 (무방향): 4(4-1)/2 = **6개**
- G6 (방향): 4(4-1) = **12개**

#### ④ 부분 그래프 (Subgraph)
- 원래 그래프에서 정점이나 간선을 **일부 제외**하여 만든 그래프

#### ⑤ 가중 그래프 (Weight Graph, 네트워크)
- 간선에 **가중치(weight)를 할당**한 그래프

---

### 그래프 관련 용어

| 용어 | 설명 |
|------|------|
| 인접(adjacent) | 두 정점 Vi, Vj를 연결하는 간선이 있을 때 |
| 부속(incident) | 간선 (Vi,Vj)는 정점 Vi와 Vj에 부속 |
| 차수(degree) | 정점에 부속된 **간선의 수** |
| 진입차수(in-degree) | 방향 그래프에서 정점을 **머리**로 하는 간선 수 |
| 진출차수(out-degree) | 방향 그래프에서 정점을 **꼬리**로 하는 간선 수 |
| 경로(path) | 정점 Vi에서 Vj까지 간선으로 연결된 정점들의 순서 |
| 경로길이(path length) | 경로를 구성하는 **간선의 수** |
| 단순경로(simple path) | 모두 **다른 정점**으로 구성된 경로 |
| 사이클(cycle) | 시작 정점 = 마지막 정점인 단순경로 |
| DAG | 방향 그래프이면서 **사이클이 없는** 그래프 |
| 연결 그래프 | 모든 정점 쌍 사이에 **경로가 있는** 그래프 |
| 단절 그래프 | 연결되지 않은 정점이 **있는** 그래프 |

---

## 2. 그래프의 구현

### ① 인접 행렬 (Adjacency Matrix) — 순차 자료구조

- n개의 정점 → **n × n 정방행렬** 사용
- 두 정점이 인접 → **1**, 아니면 **0**

| 종류 | 특징 |
|------|------|
| 무방향 그래프 | 행 i 합 = 열 i 합 = **정점 i의 차수** |
| 방향 그래프 | 행 i 합 = **진출차수**, 열 i 합 = **진입차수** |

---

### ② 인접 리스트 (Adjacency List) — 연결 자료구조

- 각 정점의 인접 정점들을 **연결 리스트**로 구성
- 리스트 내 노드는 **오름차순**으로 연결

| 종류 | 헤드 노드 배열 | 연결 노드 수 |
|------|-------------|------------|
| 무방향 그래프 | n | **2e** |
| 방향 그래프 | n | **e** |

---

## 3. 그래프의 순회

> 하나의 정점에서 시작하여 **모든 정점을 한 번씩 방문**하는 연산

### ① 깊이 우선 탐색 (DFS — Depth First Search)

- 갈 수 있는 곳까지 **깊이** 탐색 → 막히면 **되돌아와서** 다른 방향 탐색
- **스택(Stack)** 사용 (후입선출)

**탐색 순서 예시 (G9, 시작: A)**
```
A → B → D → G → E → C → F
```

**과정 요약:**
1. 시작 정점 방문 → 스택에 push
2. 방문하지 않은 인접 정점 선택 (오름차순) → 방문 + push
3. 더 이상 갈 곳 없으면 → 스택 pop → 다시 탐색
4. 스택이 공백이면 → 탐색 종료

---

### ② 너비 우선 탐색 (BFS — Breadth First Search)

- 시작 정점의 **인접 정점을 모두 먼저** 방문 → 그 다음 레벨 방문
- **큐(Queue)** 사용 (선입선출)

**탐색 순서 예시 (G9, 시작: A)**
```
A → B → C → D → E → G → F
```

**과정 요약:**
1. 시작 정점 방문 → 큐에 enQueue
2. 큐에서 deQueue → 방문하지 않은 **모든 인접 정점** 방문 + enQueue
3. 큐가 공백이면 → 탐색 종료

---

### DFS vs BFS 비교

| | DFS | BFS |
|--|-----|-----|
| 방식 | 깊이 우선 | 너비 우선 |
| 자료구조 | **스택** | **큐** |
| 비유 | 한 곳을 끝까지 파기 | 여러 곳을 고르게 파기 |

---

## 4. 신장 트리와 최소 비용 신장 트리

### 신장 트리 (Spanning Tree)

> n개의 정점, **n-1개의 간선**으로 만들어진 트리 (사이클 없음)

| 종류 | 설명 |
|------|------|
| 깊이 우선 신장 트리 | DFS로 생성된 신장 트리 |
| 너비 우선 신장 트리 | BFS로 생성된 신장 트리 |

---

### 최소 비용 신장 트리 (MST — Minimum Cost Spanning Tree)

> 신장 트리 중 **간선 가중치 합이 최소**인 트리

---

### 크루스칼 알고리즘 I (가중치 높은 간선 제거)

**순서:**
1. 간선을 **내림차순** 정렬
2. 가중치 높은 간선부터 **제거**
3. 단, 제거 시 **단절 그래프가 되면 제거 불가**
4. 간선 수가 **n-1개** 남으면 종료

---

### 크루스칼 알고리즘 II (가중치 낮은 간선 삽입)

**순서:**
1. 간선을 **오름차순** 정렬
2. 가중치 낮은 간선부터 **삽입**
3. 단, 삽입 시 **사이클이 생기면 삽입 불가**
4. 간선 수가 **n-1개** 되면 종료

---

### 프림 알고리즘 (Prim)

> 간선을 정렬하지 않고 **하나의 정점에서 시작**하여 트리를 확장

**순서:**
1. 시작 정점 선택
2. 현재 트리에 부속된 간선 중 **가중치가 가장 낮은 간선** 선택
3. 사이클이 생기면 → 다음으로 가중치 낮은 간선 선택
4. 간선 수가 **n-1개** 되면 종료

---

### 크루스칼 vs 프림 비교

| | 크루스칼 II | 프림 |
|--|-----------|------|
| 시작 | 간선 오름차순 정렬 | 시작 정점 선택 |
| 방식 | 전체 간선에서 선택 | 현재 트리에서 확장 |
| 사이클 체크 | 필요 | 필요 |

---

## 5. 최단 경로 (Shortest Path)

> 가중치 그래프에서 두 정점을 연결하는 경로 중 **가중치 합이 최소**인 경로

### 가중치 인접 행렬 (Weight Adjacent Matrix)
- 간선 없으면 → **∞ (무한대)**
- 자기 자신 → **0** (대각선)

---

### 다익스트라 알고리즘 (Dijkstra)

> **하나의 시작 정점**에서 다른 모든 정점까지의 최단 경로 (One to All)

**순서:**
1. 시작 정점을 집합 S에 추가, distance 배열 초기화
2. S에 없는 정점 중 **distance가 최소**인 정점 u를 S에 추가
3. 새로 추가된 u를 거쳐서 단축되는 경로가 있으면 **distance 수정**
4. 모든 정점이 S에 추가될 때까지 반복

---

### 플로이드 알고리즘 (Floyd)

> **모든 정점 사이**의 최단 경로 (All to All)

**순서:**
1. 인접 행렬을 배열 A에 복사하여 초기화
2. 각 정점 k를 중간 경유지로 추가하면서
3. A[i][j] vs A[i][k] + A[k][j] 비교 → **더 짧은 값으로 수정**
4. 모든 정점에 대해 반복

---

### 다익스트라 vs 플로이드 비교

| | 다익스트라 | 플로이드 |
|--|---------|--------|
| 경로 | **One to All** (단일 시작점) | **All to All** (모든 쌍) |
| 방식 | 최단 거리 정점을 집합에 추가 | 중간 경유지를 추가하며 수정 |

---

## 핵심 요약

| 개념 | 핵심 내용 |
|------|---------|
| 무방향 완전 그래프 간선 수 | n(n-1)/2 |
| 방향 완전 그래프 간선 수 | n(n-1) |
| DFS | 스택 사용, 깊이 우선 |
| BFS | 큐 사용, 너비 우선 |
| 신장 트리 | n개 정점, n-1개 간선 |
| 크루스칼 | 간선 정렬 후 선택 |
| 프림 | 정점에서 트리 확장 |
| 다익스트라 | 단일 시작점 최단 경로 |
| 플로이드 | 모든 쌍 최단 경로 |
