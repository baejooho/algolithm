# Chapter 07 트리 - 시험 핵심 정리

---

## 1. 트리 구조

### 정의

- 원소들 간에 **1:n 관계**를 가지는 **비선형 자료구조**
- 원소들 간에 계층관계를 가지는 **계층형 자료구조 (Hierarchical Data Structure)**
- 상위 원소 → 하위 원소로 내려가며 확장되는 나무 모양의 구조

### 선형 vs 비선형

| 구분 | 선형 자료구조 | 비선형 자료구조 |
|------|-------------|--------------|
| 관계 | 1:1 | 1:n (트리), n:m (그래프) |
| 예시 | 배열, 리스트, 스택, 큐 | **트리**, 그래프 |
| 특징 | 순서 있는 구조 | 계층/망 구조 |

### 트리의 구조적 특징

- 노드가 n개이면 간선은 항상 **n-1개**
- 임의의 두 노드 사이 경로는 **유일**
- **사이클(cycle) 없음**
- 루트에서 어떤 노드로 가는 경로는 오직 하나

---

## 2. 트리 용어

| 용어 | 설명 |
|------|------|
| **노드 (node)** | 트리의 원소 |
| **루트 노드 (root)** | 트리의 시작 노드 (레벨 0) |
| **간선 (edge)** | 노드를 연결하는 선. 부모-자식 관계 표현 |
| **부모 노드** | 연결된 위쪽 노드 |
| **자식 노드** | 연결된 아래쪽 노드 |
| **형제 노드 (sibling)** | 같은 부모를 가진 노드들 |
| **조상 노드 (ancestor)** | 루트까지 경로에 있는 모든 노드 |
| **자손 노드** | 서브 트리에 있는 하위 레벨의 모든 노드 |
| **단말 노드 (leaf)** | 자식이 없는 노드 (차수 = 0) |
| **서브 트리 (subtree)** | 부모와의 간선을 끊었을 때 생기는 트리 |
| **포리스트 (forest)** | 서브 트리들의 집합 |

### 차수 (degree)

- **노드의 차수** = 해당 노드에 연결된 자식 노드의 수
- **트리의 차수** = 트리 내 노드 차수 중 **가장 큰 값**

### 높이 (height) = 레벨 (level)

- **노드의 높이** = 루트에서 해당 노드까지의 간선 수
- **트리의 높이** = 트리 내 노드 높이 중 **가장 큰 값**

---

## 3. 이진 트리 (Binary Tree)

### 정의

- 모든 노드의 차수가 **2 이하**인 트리
- 모든 노드는 **왼쪽 자식**, **오른쪽 자식**만 가짐
- 공백 노드도 자식으로 취급
- `0 ≤ 노드의 차수 ≤ 2`

### 특징 (핵심 공식) ⭐

| 항목 | 공식 |
|------|------|
| 간선의 수 | **n - 1** |
| 레벨 k의 최대 노드 수 | **2^k** |
| 높이 h인 트리의 최솟값 | **h + 1** |
| 높이 h인 트리의 최댓값 | **2^(h+1) - 1** |
| 전체 최대 노드 합산 | Σ(i=0~h) 2^i = **2^(h+1) - 1** |

**최솟값 이유** : 각 레벨에 노드가 1개씩만 있으면 됨 → 레벨 0~h = (h+1)개

**최댓값 이유** : 각 레벨에 노드가 꽉 차면 레벨 i에서 2^i개 → 전부 합산

### 이진 트리의 순환적 구성

- 왼쪽/오른쪽 서브 트리도 각각 이진 트리
- → 순회 연산도 서브 트리에 대해 **순환적으로** 반복

### 종류

#### 포화 이진 트리 (Full Binary Tree)

- 모든 레벨에 노드가 **꽉 찬** 이진 트리
- 높이 h일 때 노드 수 = **2^(h+1) - 1**개
- 루트부터 차례로 **번호**가 부여됨

#### 완전 이진 트리 (Complete Binary Tree)

- 높이 h, 노드 수 n개 (n < 2^(h+1)-1)
- 포화 이진 트리의 **1번~n번 위치**와 노드 위치가 완전히 일치
- (n+1)번부터는 모두 공백 노드

#### 편향 이진 트리 (Skewed Binary Tree)

- 모든 노드가 **왼쪽 또는 오른쪽 한 방향**으로만 자식을 가짐
- 높이 h일 때 노드 수 = **h+1**개 (최솟값)
- 배열로 표현 시 **메모리 낭비** 심함

### 이진 트리 구현

#### 순차 자료구조 (1차원 배열)

- 인덱스 0번 : 사용 안 함
- 인덱스 1번 : 루트

| 관계 | 인덱스 |
|------|--------|
| 부모 노드 | `i / 2` |
| 왼쪽 자식 | `2 * i` |
| 오른쪽 자식 | `2 * i + 1` |

#### 연결 자료구조 (포인터)

- 노드 구조: `[ 왼쪽 링크 | 데이터 | 오른쪽 링크 ]`
- 자식 없으면 링크 필드에 **NULL**

### 이진 트리 순회 ⭐

| 순회 | 순서 | 특징 |
|------|------|------|
| **전위 순회 (Preorder)** | D → L → R | 루트를 **가장 먼저** / 전위 표기식 |
| **중위 순회 (Inorder)** | L → D → R | 루트를 **중간에** / 중위 표기식 |
| **후위 순회 (Postorder)** | L → R → D | 루트를 **가장 나중에** / 후위 표기식, 폴더 용량 계산 |

**수식 A\*B-C/D 순회 결과**

| 순회 | 결과 |
|------|------|
| 전위 | `- * A B / C D` |
| 중위 | `A * B - C / D` |
| 후위 | `A B * C D / -` |

---

## 4. 이진 탐색 트리 (Binary Search Tree)

### 정의

- 이진 트리 + 탐색에 최적화된 **노드 배치 규칙**
- **왼쪽 서브 트리 < 부모 노드 < 오른쪽 서브 트리**

### 탐색 연산

1. 루트에서 시작
2. 탐색 키 x와 현재 노드 비교
   - x == 노드 → **탐색 성공**
   - x < 노드 → **왼쪽** 서브 트리로 이동
   - x > 노드 → **오른쪽** 서브 트리로 이동
3. 서브 트리에서 순환적으로 반복

### 삽입 연산

1. 탐색 수행 (같은 원소 있으면 삽입 불가)
2. **탐색 실패한 위치**에 삽입

### 삭제 연산 ⭐

| 경우 | 처리 |
|------|------|
| 차수 = 0 (단말 노드) | 그냥 삭제 |
| 차수 = 1 (자식 1개) | 삭제 노드 자리를 자식 노드에게 물려줌 |
| 차수 = 2 (자식 2개) | 후계자를 선택하여 자리 물려줌 |

**후계자 선택 방법 (차수 = 2)**

- 왼쪽 서브 트리의 **가장 큰** 노드 (가장 오른쪽)
- 오른쪽 서브 트리의 **가장 작은** 노드 (가장 왼쪽)

---

## 5. 힙 트리 (Heap Tree)

### 정의

- **완전 이진 트리** 기반
- 키값이 가장 크거나 작은 노드를 빠르게 찾기 위한 자료구조

### 종류

| 종류 | 조건 | 루트 |
|------|------|------|
| **최대 힙 (max heap)** | 부모 ≥ 자식 | **최댓값** |
| **최소 힙 (min heap)** | 부모 ≤ 자식 | **최솟값** |

### 삽입 연산 ⭐

1. 완전 이진 트리 유지 → **(n+1)번 자리**에 노드 확장 후 임시 저장
2. 부모 노드와 비교 → 힙 조건 불만족 시 **자리 교환**
3. 조건 만족할 때까지 **위로 올라가며** 반복

```
① 힙 크기 증가 (i = n+1)
② item > 부모(heap[i/2]) → 자리 교환, i = i/2
③ item ≤ 부모 → 현재 위치 확정
```

### 삭제 연산 ⭐

- **루트 노드만 삭제 가능**

1. 루트 노드 원소 삭제하여 반환
2. **마지막 노드(n번)** 삭제 후, 해당 원소를 루트에 임시 저장
3. 자식 노드와 비교 → 힙 조건 불만족 시 **자리 교환**
4. 조건 만족할 때까지 **아래로 내려가며** 반복

```
① item = 루트 삭제
② temp = 마지막 노드, n--
③ temp를 루트(i=1)에 임시 저장
④ 큰 자식(j)과 비교 → temp < heap[j] → 자리 교환, i = j
⑤ temp ≥ 자식 → 현재 위치 확정 후 item 반환
```

### 구현

- **1차원 배열** 사용 (완전 이진 트리이므로)
- 인덱스 1번 = 루트

| 관계 | 인덱스 |
|------|--------|
| 부모 | `i / 2` |
| 왼쪽 자식 | `2 * i` |
| 오른쪽 자식 | `2 * i + 1` |

### 삽입 예시 (10, 45, 19, 11, 96)

| 삽입 | 힙 배열 상태 | 비고 |
|------|-------------|------|
| 10 | [_, 10] | 루트 |
| 45 | [_, 45, 10] | 45 > 10 → 교환 |
| 19 | [_, 45, 10, 19] | 19 < 45 → 유지 |
| 11 | [_, 45, 11, 19, 10] | 11 > 10 → 교환, 11 < 45 → 유지 |
| 96 | [_, 96, 45, 19, 10, 11] | 96이 루트까지 계속 올라감 |

**삭제 순서 (최대 힙):** `96 → 45 → 19 → 11 → 10` (내림차순)

---

## 핵심 공식 최종 요약 ⭐

| 항목 | 공식 |
|------|------|
| 간선 수 | n - 1 |
| 레벨 k의 최대 노드 수 | 2^k |
| 높이 h인 트리 최소 노드 수 | h + 1 |
| 높이 h인 트리 최대 노드 수 | 2^(h+1) - 1 |
| 전위 순회 | D → L → R |
| 중위 순회 | L → D → R |
| 후위 순회 | L → R → D |
| BST 삭제 후계자 | 왼쪽 서브 트리 최댓값 or 오른쪽 서브 트리 최솟값 |
| 힙 삽입 | 마지막 자리에 넣고 위로 올림 |
| 힙 삭제 | 루트 삭제, 마지막 노드를 루트에 놓고 아래로 내림 |
