# Chapter 07 트리 (Tree)

## 학습목표

- 트리의 개념을 이해한다.
- 이진 트리의 자료구조를 알아본다.
- 이진 트리의 순회를 이해한다.
- 이진 탐색 트리의 개념을 이해하고 연산 방법을 이해한다.
- 히프의 자료구조를 이해한다.

---

## 목차

1. 트리의 이해
2. 이진 트리
3. 이진 트리의 구현
4. 이진 트리의 순회
5. 이진 탐색 트리
6. 균형 이진 탐색 트리
7. 히프의 개념과 연산 및 구현

---

## 1. 트리의 이해

### 트리란?

- 원소들 간에 **1:n 관계**를 가지는 **비선형 자료구조**
- 원소들 간에 계층관계를 가지는 **계층형 자료구조(Hierarchical Data Structure)**
- 상위 원소에서 하위 원소로 내려가면서 확장되는 트리(나무) 모양의 구조

### 트리 용어 정리

| 용어 | 설명 |
|------|------|
| **노드(node)** | 트리의 원소 |
| **루트 노드(root node)** | 트리의 시작 노드 (Level 0) |
| **간선(edge)** | 노드를 연결하는 선. 부모 노드와 자식 노드를 연결 |
| **형제 노드(sibling node)** | 같은 부모 노드의 자식 노드들 |
| **조상 노드(Ancestor)** | 간선을 따라 루트 노드까지 경로에 있는 모든 노드들 |
| **자손 노드** | 서브 트리에 있는 하위 레벨의 노드들 |
| **서브 트리(subtree)** | 부모 노드와 연결된 간선을 끊었을 때 생성되는 트리 |
| **단말 노드(리프 노드)** | 차수가 0인 노드. 자식 노드가 없는 노드 |
| **포리스트(forest)** | 서브 트리의 집합 |

### 차수(degree)

- **노드의 차수** : 노드에 연결된 자식 노드의 수
  - 예) A의 차수=3, B의 차수=2, C의 차수=1
- **트리의 차수** : 트리에 있는 노드의 차수 중에서 가장 큰 값

### 높이(height)

- **노드의 높이** : 루트에서 노드에 이르는 간선의 수 (노드의 레벨)
  - 예) B의 높이=1, F의 높이=2
- **트리의 높이** : 트리에 있는 노드의 높이 중에서 가장 큰 값 (최대 레벨)

---

## 2. 이진 트리

### 이진 트리(Binary Tree) 개념

- 트리의 모든 노드의 차수를 **2 이하**로 제한 → 전체 트리의 차수가 2 이하
- 모든 노드는 **왼쪽 자식 노드**와 **오른쪽 자식 노드**만 가짐
- 부모-자식 수 관계: **1:2**
- 공백 노드도 자식 노드로 취급
- `0 ≤ 노드의 차수 ≤ 2`

### 이진 트리의 순환적 구성

- 노드의 왼쪽 자식 노드를 루트로 하는 **왼쪽 서브 트리**도 이진 트리
- 노드의 오른쪽 자식 노드를 루트로 하는 **오른쪽 서브 트리**도 이진 트리

### 일반 트리 → 이진 트리 변환

- 형제 노드들 간의 관계를 오른쪽 자식 링크로 변환

### 이진 트리의 종류

#### 포화 이진 트리(Full Binary Tree)

- 모든 레벨에 노드가 **포화상태**로 차 있는 이진 트리
- 높이가 h일 때, 최대 노드 개수: **(2^(h+1) - 1)**
- 루트를 1번으로 하여 2^(h+1)-1까지 정해진 위치에 노드 번호를 가짐

#### 완전 이진 트리(Complete Binary Tree)

- 높이가 h이고 노드 수가 n개일 때 (단, n < 2^(h+1)-1)
- 노드 위치가 포화 이진 트리에서의 노드 **1번부터 n번까지**와 완전히 일치
- (n+1)번부터 (2^(h+1)-1)번까지 노드는 모두 공백 노드

#### 편향 이진 트리(Skewed Binary Tree)

- 높이가 h일 때 h+1개의 노드를 가지면서
- 모든 노드가 **왼쪽이나 오른쪽 중 한 방향**으로만 서브 트리를 가지는 트리

---

## 3. 이진 트리의 구현

### 순차 자료구조를 이용한 구현

- **1차원 배열** 사용
- 높이가 h인 포화 이진 트리의 노드번호를 배열의 인덱스로 사용
- 인덱스 0번: 실제로 사용하지 않고 비워둠
- 인덱스 1번: 루트 저장

| 노드 관계 | 인덱스 계산 |
|-----------|-------------|
| 부모 노드 | `i / 2` |
| 왼쪽 자식 노드 | `2 * i` |
| 오른쪽 자식 노드 | `2 * i + 1` |

> ⚠️ 편향 이진 트리를 배열로 표현하면 메모리 낭비가 심함

### 연결 자료구조를 이용한 구현

- 포인터를 사용하여 이진 트리 구현
- 노드 구조: **데이터 필드 + 왼쪽 링크 필드 + 오른쪽 링크 필드**
- 자식 노드가 없으면 링크 필드에 **NULL** 저장

---

## 4. 이진 트리의 순회

### 순회(traversal) 개념

- 모든 원소를 **빠트리거나 중복하지 않고** 처리하는 연산
- 세 가지 작업: **D(현재 노드 읽기), L(왼쪽 서브 트리 이동), R(오른쪽 서브 트리 이동)**
- 이진 트리가 순환적으로 구성 → 순회도 서브 트리에 대해 순환적으로 반복
- **왼쪽 서브 트리를 오른쪽 서브 트리보다 먼저 수행**

### 전위 순회(Preorder Traversal)

- 순서: **D → L → R**
- 현재 노드를 **가장 먼저** 방문
- 수식 이진 트리 전위 순회 → **전위 표기식(prefix)** 생성

**예시 결과:** `A-B-D-H-E-I-J-C-F-G-K`

### 중위 순회(Inorder Traversal)

- 순서: **L → D → R**
- 현재 노드를 L과 R의 **중간**에 방문
- 수식 이진 트리 중위 순회 → **중위 표기식(infix)** 생성

**예시 결과:** `H-D-B-I-E-J-A-F-C-G-K`

### 후위 순회(Postorder Traversal)

- 순서: **L → R → D**
- 현재 노드를 **가장 나중에** 방문
- 수식 이진 트리 후위 순회 → **후위 표기식(postfix)** 생성
- 응용: 폴더 전체 용량 계산 (하위 먼저 계산 후 상위 계산)

**예시 결과:** `H-D-I-J-E-B-F-K-G-C-A`

### 수식 트리 순회 정리 (A*B-C/D)

| 순회 방식 | 표기 결과 |
|-----------|-----------|
| 전위 순회 | `- * A B / C D` (전위 표기식) |
| 중위 순회 | `A * B - C / D` (중위 표기식) |
| 후위 순회 | `A B * C D / -` (후위 표기식) |

### 스레드 이진 트리(Thread Binary Tree)

- **재귀호출 없이** 순회할 수 있도록 수정한 이진 트리
- 재귀호출은 시스템 스택 사용으로 비효율적
- **스레드(Thread)**: 자식 노드가 없는 경우 링크 필드를 NULL 대신 순회 순서상의 다른 노드를 가리키도록 설정
- 노드에 `isThread` 필드 추가 → 링크 필드가 자식 노드 포인터인지 스레드인지 구별
- 왼쪽 스레드: **선행자**, 오른쪽 스레드: **후속자**

---

## 5. 이진 탐색 트리

### 이진 탐색 트리(Binary Search Tree) 개념

- 이진 트리를 **탐색용 자료구조**로 사용하기 위해 원소 크기에 따라 노드 위치를 정의
- **왼쪽 서브 트리 < 부모 노드 < 오른쪽 서브 트리**

### 탐색 연산

1. 루트에서 시작
2. 탐색할 키값 x와 현재 노드 키값 비교:
   - `x == 노드 키값` → 탐색 성공
   - `x < 노드 키값` → **왼쪽** 서브 트리 탐색
   - `x > 노드 키값` → **오른쪽** 서브 트리 탐색
3. 서브 트리에 대해 순환적으로 반복

### 삽입 연산

1. 탐색 연산 수행 (같은 원소가 있으면 삽입 불가)
2. **탐색 실패한 위치**에 원소 삽입

### 삭제 연산

삭제 후 이진 탐색 트리 유지를 위한 후속 처리 필요

| 경우 | 처리 방법 |
|------|-----------|
| **차수 = 0** (단말 노드) | 그냥 삭제 |
| **차수 = 1** (자식 1개) | 삭제 노드의 자리를 자식 노드에게 물려줌 |
| **차수 = 2** (자식 2개) | 자손 중 **후계자**를 선택하여 자리 물려줌 |

**후계자 선택 방법 (차수=2인 경우)**

- 방법 1: 왼쪽 서브 트리에서 **가장 큰** 자손 노드 (가장 오른쪽 노드)
- 방법 2: 오른쪽 서브 트리에서 **가장 작은** 자손 노드 (가장 왼쪽 노드)

---

## 6. 균형 이진 탐색 트리

### AVL 트리 개념

- **Adelson-Velskii, Landis Tree** - 대표적인 균형 이진 탐색 트리
- 각 노드에서 왼쪽 서브 트리 높이(hL)와 오른쪽 서브 트리 높이(hR)의 차이가 **1 이하**
- **균형 인수(BF, Balance Factor)** = hL - hR
- 각 노드의 균형 인수: **{-1, 0, +1}** 만 허용

### 불균형 유형

| 유형 | 설명 | 해결 |
|------|------|------|
| **LL** | 불균형 노드의 왼쪽(L) 자식의 왼쪽(L)이 문제 | LL 회전 (단순 우회전) |
| **RR** | 불균형 노드의 오른쪽(R) 자식의 오른쪽(R)이 문제 | RR 회전 (단순 좌회전) |
| **LR** | 불균형 노드의 왼쪽(L) 자식의 오른쪽(R)이 문제 | LR 회전 (RR→LL 이중 회전) |
| **RL** | 불균형 노드의 오른쪽(R) 자식의 왼쪽(L)이 문제 | RL 회전 (LL→RR 이중 회전) |

### AVL 트리 회전 연산

- **단순 회전(Single Rotation)**: LL 회전, RR 회전 (1번 회전)
- **이중 회전(Double Rotation)**: LR 회전, RL 회전 (2번 회전)

> 균형 인수가 **+**이면 왼쪽 서브 트리 문제, **-**이면 오른쪽 서브 트리 문제

---

## 7. 히프(Heap)

### 히프의 개념

완전 이진 트리에서 **키값이 가장 크거나 가장 작은 노드**를 찾기 위한 자료구조

| 종류 | 설명 |
|------|------|
| **최대 히프(max heap)** | 부모 노드의 키값 ≥ 자식 노드의 키값. **루트 = 최댓값** |
| **최소 히프(min heap)** | 부모 노드의 키값 ≤ 자식 노드의 키값. **루트 = 최솟값** |

### 히프의 삽입 연산

1. 완전 이진 트리 조건 유지 → **(n+1)번 자리에 노드 확장**하여 원소 임시 저장
2. 부모 노드와 비교하여 크기 조건 불만족 시 **자리를 바꿈** (위로 올라가며 반복)

```
① 히프 크기 증가 (i = n+1)
② item과 부모 heap[i/2] 비교
③ item > heap[i/2] → 자리 교환 후 i = i/2, 반복
④ item ≤ heap[i/2] → 현재 위치 확정, heap[i] = item
```

### 히프의 삭제 연산

- 히프에서는 **루트 노드의 원소만 삭제** 가능

1. 루트 노드 원소 삭제하여 반환
2. 마지막 노드(n번)를 삭제하고, 해당 원소를 **루트에 임시 저장**
3. 임시 저장 원소를 자식 노드와 비교하며 **제자리 탐색** (아래로 내려가며 반복)

```
① item = heap[1] (루트 삭제)
② temp = heap[n], n-- (마지막 노드 제거)
③ temp를 루트(i=1)에 임시 저장
④ 왼쪽/오른쪽 자식 중 큰 값(j)과 temp 비교
⑤ temp < heap[j] → 자리 교환 후 i = j, 반복
⑥ temp ≥ heap[j] → 현재 위치 확정
⑦ item 반환
```

### 히프의 구현

- **1차원 배열(순차 자료구조)** 사용
- 인덱스 관계를 통해 부모 노드를 쉽게 찾을 수 있음

| 관계 | 인덱스 |
|------|--------|
| 부모 | `i / 2` |
| 왼쪽 자식 | `2 * i` |
| 오른쪽 자식 | `2 * i + 1` |

**삽입 예시** (10, 45, 19, 11, 96 순서로 삽입):

| 단계 | 삽입 원소 | 결과 |
|------|-----------|------|
| 1 | 10 | [_, 10] |
| 2 | 45 | [_, 45, 10] (45↔10 교환) |
| 3 | 19 | [_, 45, 10, 19] |
| 4 | 11 | [_, 45, 11, 19, 10] (11↔10 교환) |
| 5 | 96 | [_, 96, 45, 19, 10, 11] (96 계속 올라감) |

**삭제 순서:** 96 → 45 → 19 → 11 → 10 (내림차순 출력)
